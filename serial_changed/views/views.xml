<odoo>
  <data>

     <record id="partner_serial" model="ir.ui.view">
          <field name="name">Serial</field>
          <field name="model">res.partner</field>
          <field name="inherit_id" ref="base.view_partner_form"/>
          <field name="arch" type="xml">

              <field name="vat" position="after">

                   <field name="change_serial" />

              </field>

          </field>
      </record>

      <!-- Inherit Form View to Modify it -->
      <record id="invoice_warehouses" model="ir.ui.view">
          <field name="model">account.move</field>
          <field name="inherit_id" ref="account.view_move_form"/>
          <field name="arch" type="xml">

 <xpath expr="//field[@name='invoice_date']" position="after">
                      <field name="serial_name" attrs="{'invisible':[('move_type','not in',('out_refund','out_invoice'))]}"/>




              </xpath>


          </field>
      </record>
  </data>
</odoo>